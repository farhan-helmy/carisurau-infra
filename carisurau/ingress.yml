apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: carisurau-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: carisurau.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: carisurau-service
                port:
                  number: 30036